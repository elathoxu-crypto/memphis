# Offline Toggle Checklist

1. [ ] Dodaj ekran `Offline` w TUI z przełączaniem między providerami (cloud ↔ lokalny Ollama, np. `o3:mini`).
2. [ ] Opisz workflow: co robisz po przełączeniu (wpis w `journal`, `ops`, ewentualnie `project_task`).
3. [ ] Każdy toggle loguj tagami: `offline-toggle`, `agent:style`, `role:user`.
4. [ ] Automatycznie zapisuj wynik `memphis status` (cron/ops) po zmianie trybu.
5. [ ] Dodaj instrukcję do README / docs, jakie modele są dostępne i jak działa fallback chain.
